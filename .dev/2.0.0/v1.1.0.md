Make macOS users have a setup, where they are able to set a custom alias to open the CLi (e.g., 'wcli' or 'workstation') -- in settings, not a thing each time a new version comes around. If they select it, just give instructions on how with the terminal.

Improve UI/UX

Improve the app, w/o new additions unless it imrpoves the cli.

