# NOT FINAL PLAN JOTTING NOTES: Version 0.0.4
- Fix AI chat prompt to indicate its in Workstation CLI, an interface for chemistry and AI actions as said in the readme.
- When choosing an option, clear the text before it, so it only displays the CURRENT page/options not everything that came before it. Also, when exiting (both through the main menu and CTRL + C), it should clear all previous text, leaving just a clean terminal.
  - If In something like a calculator, keep the past calculations, but clear the menu/page text. So if there is multiple steps (like molar mass), and they need to refer to earlier steps, they still can, but the information from HOME --> Chemistry --> Molar Mass is cleared, and it ONLY shows Molar Mass calculations (current and past results), and then when they go back, it clears that, and shows Chemistry home page.
- Add credits to the CLI Information page, giving credit to Ollama for the ability to use Local models, and giving a universal credit for those creating the AI models used (Gemma, Mistral, etc.).
- Add a tab system (maybe top of the page?) that shows the current location in the CLI. Similar to how some programs show you your current directory (for example, in this case, it would show HOME --> Chemistry --> Molar Mass).
- Refine Readme with Shields.io banners.
- Re-word the exit message, making it less "script like" and more personal, and less formal.
- Add a new sub-menu to the chemistry section for "Periodic Table". When they select this, it shows the periodic table of elements (maybe simplified?). It would also include the atomic number, name, symbol, and atomic mass for each element.
- Input validation & error handling improvements — Add user-friendly error messages and input validation (e.g., handling invalid chemical formulas, catching bad numeric inputs). Small quality-of-life win that makes the CLI feel more polished.
- Command history/shortcuts — Let users quickly re-run recent calculations or commands (like !1 for the last calculation). Especially useful for chemistry workflows where you might repeat similar molar mass or stoichiometry calculations.
- Keyboard navigation improvements — Add arrow key support to navigate menu options instead of just typing numbers. Feels more modern and reduces typos.
- Help/tooltip system — Quick ? or help command available in most menus that explains what each option does. Light but helpful.
- Save/export calculations — Let users save calculation results to a simple text file or clipboard. Particularly useful for chemistry work that might need to be referenced later.
- Toggle for UI changes, like Light/Dark Mode. Only do light/dark mode in implementation for now.

