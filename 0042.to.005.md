Confirmed features:

Change "View Config File Location" in "Settings" to open the folder in Finder for MacOS, if on windows, say where the file location is. This is because the user currently has no way to access the config file in Windows.

Fix an issue if you click a button that isnt a arrow, it will display strings inside the open space below the CLI to not allow for functionality after: ^[[A^[[B. The user is unable to fix this without closing and reopening the CLI.

When trying to Edit Local Config File in the AI section, it saysNo EDITOR/VISUAL environment variable is set, so the file was not opened automatically.
Edit this file manually, then restart or reopen this menu: /Users/caden/.workstation_cli/config.json

This is not true. If you go to settings and edit the config file location, it will open the folder in Finder and you can edit the file there.


Add themes of: Red, Orange, Green, Cyan, Dark Blue (replaces current blue), Pink (replaces current pink), and a Black theme, which is black text on a black background. For black theme, use #000000 for black text, #FFFFFF for white background, #FFFFFF for title background, #FFFFFF for selected background, #FFFFFF for cursor color, #FFFFFF for border color, #FFFFFF for footer color, #FFFFFF for header color, #FFFFFF for info color, #FFFFFF for ok color, #FFFFFF for warn color, #FFFFFF for error color. And For Green theme, use #00FF00 for green text, #000000 for black background, #000000 for title background, #000000 for selected background, #000000 for cursor color, #000000 for border color, #000000 for footer color, #000000 for header color, #000000 for info color, #000000 for ok color, #000000 for warn color, #000000 for error color. 


**BIG FEATURES**


1. Expand  past Chem and AI, create a new section for Graphing! Make this the best graphing tool ever. Nest under its own seciton Main menu. It should allow the user to create a function f(x) = x^2, and more complex stuff too!